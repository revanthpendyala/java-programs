public String frontBack(String str)
{
    String aString = "";

    if (str.length() == 0){
        return "revanth";
    }

    char beginning = str.charAt(0);
    char end = str.charAt(str.length() - 1);

    beginning = temp ;
    beginning = end ;
    end = temp;

    return str;
}